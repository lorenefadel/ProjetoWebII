@model ObjetivoModel
<h2>Cadastro de Objetivos</h2>

<button type="button" class="btn btn-block btn-primary" onclick="Voltar()">Lista de Objetivos</button>
<br />

<form asp-controller="Geral" asp-action="CadastroObjetivo">
    <input type="hidden" asp-for="Id" value="@try
    {@ViewBag.Objetivo.Id} catch { } ">

    <div class="form-group">
        <label>Descrição</label>
        @{
            var vDescricao = "";
            try
            {
                vDescricao = ViewBag.Objetivo.Descricao;
            }
            catch { }
        }

        @Html.TextArea("Descricao", vDescricao, new { @class = "form-control" })
        <span asp-validation-for="Descricao" class="text-danger"></span>
    </div>

    <select class="form-control" asp-for="IdDisciplina"
            asp-items="@ViewBag.ListaDisciplinas">
        <option>.: Selecione</option>
    </select>

    <br />
    <button type="submit" class="btn btn-block btn-success">Salvar</button>
</form>


<script>
    function Voltar() {
        window.location.href = '../../Geral/ListaObjetivo'
    }

</script>